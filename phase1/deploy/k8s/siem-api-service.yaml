apiVersion: v1
kind: Service
metadata:
  name: siem-api
  namespace: conflict-monitor
spec:
  type: ClusterIP
  selector:
    app: siem-api
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP

